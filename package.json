{
  "name": "blog",
  "version": "1.0.0",
  "description": "this is a blog with vuepress",
  "main": "index.html",
  "scripts": {
    "test": "test",
    "build": "vuepress build docs",
    "dev-server": "lite-server --baseDir='.vuepress/dist'",
    "publish": "push-dir --dir=.vuepress/dist --branch=gh-pages --cleanup"
  },
  "repository": {
    "type": "git",
    "url": "git"
  },
  "keywords": [
    "blog"
  ],
  "author": "cmtp",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.4.0",
    "push-dir": "^0.4.1"
  }
}
